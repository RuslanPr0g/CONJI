import { AddToPrefixPipe } from './add-to-prefix.pipe';

describe('AddToPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new AddToPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
